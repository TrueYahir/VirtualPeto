<Window x:Class="VirtualPeto.Tools.AudioConverterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Audio Format Converter" Height="250" Width="550"
        Background="#1E1E2A" Foreground="White"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Input File:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="0,0,15,20" FontWeight="SemiBold"/>
        <TextBox x:Name="TxtInputFile" Grid.Row="0" Grid.Column="1" Height="28" Margin="0,0,10,20" 
                 Background="#252538" Foreground="White" BorderBrush="#444" Padding="5,4" IsReadOnly="True"/>
        <Button x:Name="BtnBrowse" Content="Browse..." Grid.Row="0" Grid.Column="2" Width="90" Height="28" Margin="0,0,0,20" 
                Background="#4A90E2" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnBrowse_Click"/>

        <TextBlock Text="Target Format:" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" Margin="0,0,15,20" FontWeight="SemiBold"/>
        <ComboBox x:Name="CmbTargetFormat" Grid.Row="1" Grid.Column="1" Height="28" Margin="0,0,10,20" 
                Foreground="White" Background="#252538" BorderBrush="#444" HorizontalAlignment="Left" Width="120">
    
            <ComboBox.Style>
                <Style TargetType="ComboBox">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <ToggleButton x:Name="ToggleButton" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                Background="{TemplateBinding Background}" 
                                                BorderThickness="1" 
                                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                    <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="White" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    
                                    <ContentPresenter IsHitTestVisible="False" 
                                                    Content="{TemplateBinding SelectionBoxItem}" 
                                                    Margin="8,4,25,4" 
                                                    VerticalAlignment="Center" 
                                                    HorizontalAlignment="Left"/>
                                                    
                                    <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                        <Border Background="#252538" BorderBrush="#444" BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="200">
                                            <ScrollViewer>
                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ComboBox.Style>

            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="#252538"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="8,5"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="#4A90E2"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ComboBox.ItemContainerStyle>

            <ComboBoxItem Content=".wav" IsSelected="True"/>
            <ComboBoxItem Content=".mp3"/>
            <ComboBoxItem Content=".ogg"/>
            <ComboBoxItem Content=".flac"/>
        </ComboBox>

        <Button x:Name="BtnConvert" Content="Convert Audio" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" 
                Width="180" Height="40" Background="#4A90E2" Foreground="White" BorderThickness="0" 
                FontWeight="Bold" Cursor="Hand" Click="BtnConvert_Click" VerticalAlignment="Bottom"/>
    </Grid>
</Window>